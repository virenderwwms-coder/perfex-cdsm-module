<?php defined('BASEPATH') or exit('No direct script access allowed'); ?>

<div class="container-fluid cdsm-container">
    <!-- Statistics Cards -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="cdsm-stats-card">
                <div class="number" style="color: #dc3545;"><?php echo count($reminders_30_days); ?></div>
                <div class="label">Due in 30 Days</div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="cdsm-stats-card">
                <div class="number" style="color: #ffc107;"><?php echo count($reminders_15_days); ?></div>
                <div class="label">Due in 15 Days</div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="cdsm-stats-card">
                <div class="number" style="color: #fd7e14;"><?php echo count($reminders_7_days); ?></div>
                <div class="label">Due in 7 Days</div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="cdsm-stats-card">
                <div class="number" style="color: #8b0000;"><?php echo count($expired_reminders); ?></div>
                <div class="label">Expired</div>
            </div>
        </div>
    </div>

    <!-- Reminders by Category -->
    <div class="row">
        <!-- 30 Days Before Expiry -->
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header" style="background-color: #dc3545; color: white;">
                    <h5 class="card-title mb-0">30 Days Before Expiry</h5>
                </div>
                <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                    <?php if (!empty($reminders_30_days)) { ?>
                        <?php foreach ($reminders_30_days as $reminder) { ?>
                            <div class="cdsm-reminder-card">
                                <div class="d-flex justify-content-between">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1"><?php echo htmlspecialchars($reminder->document_name); ?></h6>
                                        <p class="mb-1"><strong><?php echo htmlspecialchars($reminder->customer_name); ?></strong></p>
                                        <small class="text-muted">Expires: <?php echo _d($reminder->expiry_date); ?></small>
                                    </div>
                                    <div>
                                        <?php if (!$reminder->is_notified) { ?>
                                            <button class="btn btn-sm btn-primary" onclick="markReminderNotified(<?php echo $reminder->id; ?>)">
                                                Mark Done
                                            </button>
                                        <?php } else { ?>
                                            <span class="badge badge-success">Done</span>
                                        <?php } ?>
                                    </div>
                                </div>
                            </div>
                        <?php } ?>
                    <?php } else { ?>
                        <p class="text-muted">No reminders at this stage</p>
                    <?php } ?>
                </div>
            </div>
        </div>

        <!-- 15 Days Before Expiry -->
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header" style="background-color: #ffc107; color: #333;">
                    <h5 class="card-title mb-0">15 Days Before Expiry</h5>
                </div>
                <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                    <?php if (!empty($reminders_15_days)) { ?>
                        <?php foreach ($reminders_15_days as $reminder) { ?>
                            <div class="cdsm-reminder-card warning">
                                <div class="d-flex justify-content-between">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1"><?php echo htmlspecialchars($reminder->document_name); ?></h6>
                                        <p class="mb-1"><strong><?php echo htmlspecialchars($reminder->customer_name); ?></strong></p>
                                        <small class="text-muted">Expires: <?php echo _d($reminder->expiry_date); ?></small>
                                    </div>
                                    <div>
                                        <?php if (!$reminder->is_notified) { ?>
                                            <button class="btn btn-sm btn-warning" onclick="markReminderNotified(<?php echo $reminder->id; ?>)">
                                                Mark Done
                                            </button>
                                        <?php } else { ?>
                                            <span class="badge badge-success">Done</span>
                                        <?php } ?>
                                    </div>
                                </div>
                            </div>
                        <?php } ?>
                    <?php } else { ?>
                        <p class="text-muted">No reminders at this stage</p>
                    <?php } ?>
                </div>
            </div>
        </div>
    </div>

    <!-- 7 Days and Expired -->
    <div class="row">
        <!-- 7 Days Before Expiry -->
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header" style="background-color: #fd7e14; color: white;">
                    <h5 class="card-title mb-0">7 Days Before Expiry ⚠️</h5>
                </div>
                <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                    <?php if (!empty($reminders_7_days)) { ?>
                        <?php foreach ($reminders_7_days as $reminder) { ?>
                            <div class="cdsm-reminder-card critical">
                                <div class="d-flex justify-content-between">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1"><?php echo htmlspecialchars($reminder->document_name); ?></h6>
                                        <p class="mb-1"><strong><?php echo htmlspecialchars($reminder->customer_name); ?></strong></p>
                                        <small class="text-muted">Expires: <?php echo _d($reminder->expiry_date); ?></small>
                                    </div>
                                    <div>
                                        <?php if (!$reminder->is_notified) { ?>
                                            <button class="btn btn-sm btn-danger" onclick="markReminderNotified(<?php echo $reminder->id; ?>)">
                                                Mark Done
                                            </button>
                                        <?php } else { ?>
                                            <span class="badge badge-success">Done</span>
                                        <?php } ?>
                                    </div>
                                </div>
                            </div>
                        <?php } ?>
                    <?php } else { ?>
                        <p class="text-muted">No reminders at this stage</p>
                    <?php } ?>
                </div>
            </div>
        </div>

        <!-- Expired Documents -->
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header" style="background-color: #8b0000; color: white;">
                    <h5 class="card-title mb-0">🔴 Expired Documents</h5>
                </div>
                <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                    <?php if (!empty($expired_reminders)) { ?>
                        <?php foreach ($expired_reminders as $reminder) { ?>
                            <div class="cdsm-reminder-card critical">
                                <div class="d-flex justify-content-between">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1"><?php echo htmlspecialchars($reminder->document_name); ?></h6>
                                        <p class="mb-1"><strong><?php echo htmlspecialchars($reminder->customer_name); ?></strong></p>
                                        <small class="text-muted">Expired: <?php echo _d($reminder->expiry_date); ?></small>
                                    </div>
                                    <div>
                                        <?php if (!$reminder->is_notified) { ?>
                                            <button class="btn btn-sm btn-dark" onclick="markReminderNotified(<?php echo $reminder->id; ?>)">
                                                Mark Done
                                            </button>
                                        <?php } else { ?>
                                            <span class="badge badge-success">Done</span>
                                        <?php } ?>
                                    </div>
                                </div>
                            </div>
                        <?php } ?>
                    <?php } else { ?>
                        <p class="text-muted">No expired documents</p>
                    <?php } ?>
                </div>
            </div>
        </div>
    </div>

    <!-- All Reminders Table -->
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">All Reminders</h5>
                </div>
                <div class="card-body">
                    <?php if (!empty($all_reminders)) { ?>
                        <div class="table-responsive">
                            <table class="table table-hover table-sm">
                                <thead>
                                    <tr>
                                        <th>Document</th>
                                        <th>Customer</th>
                                        <th>Days Before</th>
                                        <th>Reminder Date</th>
                                        <th>Expiry Date</th>
                                        <th>Status</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php foreach ($all_reminders as $reminder) { ?>
                                        <tr>
                                            <td><?php echo htmlspecialchars($reminder->document_name); ?></td>
                                            <td><?php echo htmlspecialchars($reminder->customer_name); ?></td>
                                            <td><?php echo $reminder->reminder_days; ?> days</td>
                                            <td><?php echo _d($reminder->reminder_date); ?></td>
                                            <td><?php echo _d($reminder->expiry_date); ?></td>
                                            <td>
                                                <?php if ($reminder->is_notified) { ?>
                                                    <span class="badge badge-success">Notified</span>
                                                <?php } else { ?>
                                                    <span class="badge badge-warning">Pending</span>
                                                <?php } ?>
                                            </td>
                                            <td>
                                                <?php if (!$reminder->is_notified) { ?>
                                                    <button class="btn btn-xs btn-primary" onclick="markReminderNotified(<?php echo $reminder->id; ?>)">
                                                        Mark Done
                                                    </button>
                                                <?php } ?>
                                            </td>
                                        </tr>
                                    <?php } ?>
                                </tbody>
                            </table>
                        </div>
                    <?php } else { ?>
                        <div class="alert alert-info">No reminders found</div>
                    <?php } ?>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    <?php $this->load->view('cdsm/assets/js/cdsm'); ?>
</script>
